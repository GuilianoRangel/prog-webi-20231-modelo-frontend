/**
 * Classe de trânsferencia com os parâmetros utilizados em filtros de pesquisa de Amigo.
 *
 * @author Guiliano Rangel (UEG)
 */
export class GrupoStats {
  id!: number;
  nome!: string;
  descricao!: string;
  totalUsuarios!: number;
}
